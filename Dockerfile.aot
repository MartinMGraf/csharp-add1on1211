# syntax=docker/dockerfile:1
FROM cr.microsoft.com/dotnet/nightly/sdk:9.0-alpine-aot-arm64v8

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        clang \
        llvm \
        zlib1g-dev \
    && rm -rf /var/lib/apt/lists/*
